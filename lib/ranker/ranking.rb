module Ranker

  class Ranking

    attr_reader :rankings, :index, :rank, :score, :values

    def initialize(rankings, index, rank, score, values)
      @rankings = rankings
      @index = index
      @rank = rank
      @score = score
      @values = values
    end

    # Properties:


  end # class

end # module

